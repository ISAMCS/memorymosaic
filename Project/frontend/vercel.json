{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install --legacy-peer-deps && npm run dev --prefix ."
      }
    },
    {
      "src": "api/server.js",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install --legacy-peer-deps && node api/server.js"
      }
    },
    {
      "src": "api/models.js",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install --legacy-peer-deps && node api/models.js"
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://memorymosaic.vercel.app/api/$1" },
    { "src": "/model/(.*)", "dest": "api/models.js" },
    { "src": "/(.*)", "dest": "$1" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}